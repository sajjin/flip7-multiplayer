# Public port exposed by Docker on your server
HOST_PORT=3000

# Internal app port inside the flip7 container
APP_PORT=3000

# Cloudflare Tunnel token from Zero Trust dashboard
# https://one.dash.cloudflare.com/ -> Networks -> Tunnels -> <your tunnel> -> Docker
CLOUDFLARE_TUNNEL_TOKEN=replace-with-your-token
